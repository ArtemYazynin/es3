import { ApplicantTypePipe } from './applicant-type.pipe';

describe('ApplicantTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ApplicantTypePipe();
    expect(pipe).toBeTruthy();
  });
});
