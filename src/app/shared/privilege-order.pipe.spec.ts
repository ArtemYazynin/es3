import { PrivilegeOrderPipe } from './privilege-order.pipe';

describe('PrivilegeOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new PrivilegeOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
